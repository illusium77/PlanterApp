using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using PlanterApp.Domain;
using PlanterApp.Applications.ViewModels;
using System.ComponentModel.Composition.Hosting;

namespace UnitTests
{
    [TestClass]
    public class PlantViewModelUnitTest
    {

        [TestMethod]
        public void TestPlantSwitch()
        {
            //var settings = new AppSettings();

            //var plantA = new Plant(1) { Species = SpeciesType.ML, Population = PopulationType.YCN, Family = 6 };
            //var plantB = new Plant(2) { Species = SpeciesType.MG, Population = PopulationType.SHG, Family = 6 };
        }
    }
}
