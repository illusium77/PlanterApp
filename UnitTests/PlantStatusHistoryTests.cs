using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using PlanterApp.Domain;

namespace UnitTests
{
    [TestClass]
    public class PlantStatusHistoryTests
    {
        [TestMethod]
        public void TestPlantStatusHistory()
        {
            //var history = new PlantStatusHistory();

            //history.SetStatus(PlantState.Planted);
            //history.SetStatus(PlantState.Alive);

            //history.AllStatuses[1].TimeStamp = DateTime.Now.AddDays(-1);
            //Assert.IsTrue(history.AllStatuses[0].State == PlantState.Alive);
        }
    }
}
